{
  "name": "lookbook-website",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-static",
    "copy-static": "cp index.html how-it-works.html google-pipeline.html analytics.html examples.html pricing.html faq.html privacy.html terms.html white-papers.html dist/ && cp -r css dist/ && cp -r js dist/ && cp -r public dist/ && cp -r assets dist/ 2>/dev/null || true",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^5.0.0"
  }
}
